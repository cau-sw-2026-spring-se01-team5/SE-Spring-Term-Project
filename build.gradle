subprojects {
    buildDir = new File(System.getProperty('java.io.tmpdir'), "issue-tracker-build/${project.name}")

    tasks.withType(JavaCompile).configureEach {
        options.encoding = 'UTF-8'
    }
}
